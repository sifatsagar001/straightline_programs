{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Straightline Programs",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}",
        }
    ]
}
